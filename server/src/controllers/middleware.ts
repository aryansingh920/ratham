import { home } from "../middleware/home";
import { login } from "../middleware/login";
import { encryptGet } from "../middleware/encryptStr";
import { decryptGet } from "../middleware/decryptStr";

export { home, login, encryptGet, decryptGet };
